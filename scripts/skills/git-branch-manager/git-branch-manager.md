# Skill git-branch-manager — Cápsula de implementación

**skillId:** git-branch-manager  
**Ruta canónica:** Cúmulo `paths.skillCapsules["git-branch-manager"]` (`scripts/skills/git-branch-manager/`)

## Uso (humano)

```powershell
.\scripts\skills\git-branch-manager\Git-Branch-Manager.bat --branch-name "feat/mi-rama" --create
.\scripts\skills\git-branch-manager\Git-Branch-Manager.bat --branch-name "main"
```

## Uso (agente / IA)

Invocar `git_branch_manager.exe` con envelope JSON v2.

