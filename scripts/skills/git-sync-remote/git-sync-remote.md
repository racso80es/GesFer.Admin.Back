# Skill git-sync-remote — Cápsula de implementación

**skillId:** git-sync-remote  
**Ruta canónica:** Cúmulo `paths.skillCapsules["git-sync-remote"]` (`scripts/skills/git-sync-remote/`)

## Uso (humano)

```powershell
.\scripts\skills\git-sync-remote\Git-Sync-Remote.bat
.\scripts\skills\git-sync-remote\Git-Sync-Remote.bat --force
```

## Uso (agente / IA)

Invocar `git_sync_remote.exe` con envelope JSON v2.

