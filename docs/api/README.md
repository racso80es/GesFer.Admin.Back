# Contratos API (exportación)

| Archivo | Descripción |
|---------|-------------|
| [geolocation-api-dtos.export.json](./geolocation-api-dtos.export.json) | Endpoints `GET /api/geolocation/*`, DTOs de lectura (`*GeoReadDto`), ejemplos y fragmentos JSON Schema. |

La fuente de verdad del código sigue siendo `src/GesFer.Admin.Back.Application/DTOs/Geo/GeoDtos.cs` y `GeolocationController.cs`. Actualizar el `.export.json` si cambian propiedades o rutas.
