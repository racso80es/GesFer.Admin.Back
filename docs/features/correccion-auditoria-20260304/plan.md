# Plan
1. Modificar `GesFer.Admin.Back.Api.csproj`.
2. Remover el bloque `PackageReference` a `Microsoft.EntityFrameworkCore.Design`.
3. Validar con `dotnet build` y `dotnet test`.
4. Llenar `implementation.md`.
